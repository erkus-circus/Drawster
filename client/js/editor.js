/* /editor.js */
(function (window) {
    window.Project.prototype.Editor = {
        zoomOut: function () {
            alert('zoomed in')
        },
        zoomIn: function () {
            alert('zoomed in')
        }
    };
})(window);