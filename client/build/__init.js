/* /build/__init.js */
/*
 * All ID on any elem is a generated ID, so when shit+click it selects it.
 * if user tries to do an operation when not all selected IDs are of same type, error.
 */

(function (window) {
    window.Project = window.Project || class {
        constructor() {};
    };
})(window);